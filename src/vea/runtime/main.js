import vue from "vue"
import vueRouter from "vue-router"
import vuex, {mapActions, mapState} from "vuex"
import common from "../extends/common.js"

const extendVue = common
export {vue, vueRouter, vuex, mapActions, mapState, extendVue}
